# Configuration de la base de données PostgreSQL
POSTGRES_USER=kairn_user
POSTGRES_PASSWORD=kairn_password
POSTGRES_DB=kairn_db

# URL de connexion pour le backend (FastAPI)
# Le nom d'hôte 'db' correspond au nom du service dans docker-compose.yml
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}

# Configuration du frontend (Next.js)
# Assure que le frontend sait où trouver l'API backend
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000